import { ExtraFormControlPipe } from './extra-form-control.pipe';

describe('ExtraFormControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraFormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
