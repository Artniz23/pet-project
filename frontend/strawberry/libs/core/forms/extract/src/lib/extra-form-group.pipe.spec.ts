import { ExtraFormGroupPipe } from './extra-form-group.pipe';

describe('ExtraFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
